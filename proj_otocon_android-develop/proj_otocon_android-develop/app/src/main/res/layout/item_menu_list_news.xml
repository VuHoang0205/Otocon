<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="menuNewModel"
            type="org.atmarkcafe.otocon.model.menunew.MenuNewModel" />

        <variable
            name="isCheck"
            type="android.databinding.ObservableBoolean" />

    </data>

    <android.support.constraint.ConstraintLayout
        android:id="@+id/layout_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@{menuNewModel.isReaded() ? @drawable/xml_background_white_item_menu_list_new : @drawable/xml_background_gray_item_menu_list_new }"
        android:foreground="?android:attr/selectableItemBackground"
        android:paddingBottom="15dp">

        <View
            android:id="@+id/view"
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:background="#ABABAB"
            app:layout_constraintTop_toBottomOf="@+id/textView"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvDateAt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="15dp"
            android:layout_marginEnd="15dp"
            android:text="@{menuNewModel.formatDate()}"
            android:textColor="#0462A8"
            android:textSize="9sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvTitle"
            style="@style/text_bold"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:lineSpacingExtra="7sp"
            android:singleLine="@{isCheck}"
            android:text="@{menuNewModel.title}"
            android:textAlignment="viewStart"
            android:textColor="#000000"
            android:textSize="14sp"
            app:layout_constraintBaseline_toBaselineOf="@+id/tvDateAt"
            app:layout_constraintEnd_toStartOf="@+id/tvDateAt"
            app:layout_constraintStart_toStartOf="parent" />

        <TextView
            android:id="@+id/tvSubContent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_marginTop="3dp"
            android:layout_marginEnd="15dp"
            android:autoLink="web"
            android:ellipsize="end"
            android:lineSpacingExtra="7sp"
            android:singleLine="@{isCheck}"
            android:text="@{menuNewModel.sub_content}"
            android:textAlignment="viewStart"
            android:textColor="#000000"
            android:textSize="14sp"
            android:visibility="@{menuNewModel.showTextString(menuNewModel.sub_content) ? View.VISIBLE : View.GONE}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tvTitle" />

        <TextView
            android:id="@+id/tvContent"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_marginTop="3dp"
            android:layout_marginEnd="15dp"
            android:autoLink="web"
            android:ellipsize="end"
            android:lineSpacingExtra="7sp"
            android:singleLine="@{isCheck}"
            android:text="@{menuNewModel.readHtml()}"
            android:textAlignment="viewStart"
            android:textColor="#000000"
            android:textSize="14sp"
            app:layout_constraintBottom_toTopOf="@+id/textView"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tvSubContent" />

        <TextView
            android:id="@+id/textView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_marginTop="3dp"
            android:layout_marginEnd="15dp"
            android:ellipsize="end"
            android:lineSpacingExtra="6dp"
            android:singleLine="@{isCheck}"
            android:text="@{menuNewModel.linkAction}"
            android:textAlignment="viewStart"
            android:textColor="#F7648F"
            android:textSize="12sp"
            android:visibility="@{menuNewModel.showTextSpannable(menuNewModel.linkAction) ? View.VISIBLE : View.GONE}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/tvContent" />

    </android.support.constraint.ConstraintLayout>
</layout>

