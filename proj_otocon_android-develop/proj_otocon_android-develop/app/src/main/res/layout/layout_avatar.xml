<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>

        <variable
            name="fragment"
            type="org.atmarkcafe.otocon.function.mypage.MyPageMenuFragment" />

        <variable
            name="canDelete"
            type="Boolean" />

        <variable
            name="position"
            type="Integer" />

        <variable
            name="size"
            type="Integer" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="10dp"
        android:paddingEnd="10dp">


        <View
            android:id="@+id/avatar_background"
            android:layout_width="48.5dp"
            android:layout_height="48.5dp"
            android:background="@drawable/xml_oval_white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <android.support.v7.widget.CardView
            android:layout_width="45dp"
            android:layout_height="45dp"
            app:cardCornerRadius="22.5dp"
            app:cardElevation="0dp"
            app:contentPadding="0dp"
            app:layout_constraintBottom_toBottomOf="@+id/avatar_background"
            app:layout_constraintEnd_toEndOf="@+id/avatar_background"
            app:layout_constraintStart_toStartOf="@+id/avatar_background"
            app:layout_constraintTop_toTopOf="@+id/avatar_background">

            <ImageView
                android:id="@+id/avatar"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:alpha="@{(position>size+1)? 0.5f: 1.0f}"
                android:onClick="@{()->fragment.changeGallery(position)}"
                android:scaleType="centerCrop"
                android:src="@drawable/ic_take_photo" />

        </android.support.v7.widget.CardView>

    </android.support.constraint.ConstraintLayout>
</layout>
